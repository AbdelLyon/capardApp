{% extends 'base.html.twig' %}

{% block title %}Hello HomeController!
{% endblock %}

{% block body %}

	<div class="flex-fill ms-4 shadow table-responsive bg-white p-4">
		<div class="d-flex justify-content-between align-items-center mb-5">
			<a href="{{path('admin_subcategory_readAll')}}">
				<i class="angle fa-solid fa-angle-left"></i>
			</a>
			<h2 class="fw-bold text-secondary m-0">Détails</h2>
		</div>
		<div class="container">
			<table class="table table-sm table-hover">
				<tbody>
					<tr class="align-center">
						<th class="text-primary">ID :</th>
						<td class="content-id">{{ subcategory.id }}</td>
					</tr>
					<tr class="align-center">
						<th class="text-primary">TITRE :</th>
						<td>{{ subcategory.name }}</td>
					</tr>

				</tbody>
			</table>
			<div class="d-flex flex-row-reverse">
				<a href=" {{path('admin_subcategory_delete', {id: subcategory.id})}} " class="btn btn-sm btn-danger d-flex mb-2">
					<i class="fa-solid fa-trash-can text-white"></i>
				</a>
			</div>
		</div>
	</div>
{% endblock %}
